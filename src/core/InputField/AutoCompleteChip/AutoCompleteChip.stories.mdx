import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { AutoCompleteChip } from "./AutoCompleteChip";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { testData } from "./testData";

<Meta title="Core/InputField/AutoCompleteChip" component={AutoCompleteChip} />

# AutoCompleteChip

`AutoCompleteChip` is a normal text input enhanced by a panel of suggested options.

<Canvas>
  <Story
    name="Default"
    args={{
      onChange: (event, value) => console.log(value),
      options: testData,
      label: "label",
      placeholder: "placeholder",
      multiple: true,
      disableCloseOnSelect: true,
    }}
  >
    {(args) => (
      <div style={{ width: "30rem", height: "10rem" }}>
        <AutoCompleteChip {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={AutoCompleteChip} />
