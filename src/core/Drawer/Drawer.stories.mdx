import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Drawer } from "../Drawer";
import { ButtonFilled, ButtonOutlined } from "../Button";
import { useState } from "react";
import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Core/Drawer" component={Drawer} />

# Drawer

`Drawer` modal component provides a solid foundation for creating dialogs, popovers,
lightboxes, or whatever else.

## Default variant:

<Canvas>
  <Story
    name="Default"
    args={{ variant: "temporary", anchor: "left", icon: "close" }}
  >
    {(args) => {
      const [open, setOpen] = useState(false);
      return (
        <div>
          <ButtonFilled onClick={() => setOpen(true)}>Open Drawer</ButtonFilled>
          <Drawer
            {...args}
            open={open}
            onClose={() => setOpen(false)}
            onButtonClose={() => setOpen(false)}
          >
            <div
              style={{
                padding: "4rem",
                fontSize: "2rem",
              }}
            >
              This is a Drawer
            </div>
          </Drawer>
        </div>
      );
    }}
  </Story>
</Canvas>

## Other variants:

<ArgsTable of={Drawer} />
