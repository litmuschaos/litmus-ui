import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { OutlinedPills } from "../OutlinedPills";

import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Core/OutlinedPills" component={OutlinedPills} />

## OutlinedPills

`Buttons` allow users to take actions, and make choices, with a single tap.
`OutlinedPills` is a button with solid background.

## Default variant:

<Canvas>
  <Story name="Default" args={{ label: "Outlined Pill" }}>
    {(args) => <OutlinedPills {...args} />}
  </Story>
</Canvas>

## Other variants:

<ArgsTable of={OutlinedPills} />
