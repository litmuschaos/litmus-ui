import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { OutlinedPills } from "../OutlinedPills";

import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta
  title="Core/OutlinedPills"
  component={OutlinedPills}
  argTypes={{
    variant: {
      control: {
        type: "radio",
        options: {
          running: "running",
          pending: "pending",
          failed: "failed",
          succeeded: "succeeded",
        },
        defaultValue: "succeeded",
      },
    },
  }}
/>

# OutlinedPills

`OutlinedPills` component provides a border to its children.

## Default variant:

<Canvas>
  <Story name="Default" args={{ label: "succeeded", variant: "succeeded" }}>
    {(args) => <OutlinedPills {...args} />}
  </Story>
</Canvas>

## Other variants:

<ArgsTable of={OutlinedPills} />
