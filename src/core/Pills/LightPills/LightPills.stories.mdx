import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { LightPills } from "../LightPills";

import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta
  title="Core/LightPills"
  component={LightPills}
  argTypes={{
    variant: {
      control: {
        type: "radio",
        options: {
          success: "success",
          warning: "warning",
          danger: "danger",
        },
        defaultValue: "default",
      },
    },
    onClick: { action: "clicked" },
  }}
/>

# LightPills

`LightPills` component provide a solid background to its children with four variation.

## Default variant:

<Canvas>
  <Story name="Default" args={{ label: "Light Pill" }}>
    {(args) => <LightPills {...args} />}
  </Story>
</Canvas>

## Other variants:

<Canvas>
  <Story name="Success" args={{ variant: "success", label: "Success" }}>
    {(args) => <LightPills {...args} />}
  </Story>{" "}
  <Story name="Warning" args={{ variant: "warning", label: "Warning" }}>
    {(args) => <LightPills {...args} />}
  </Story>
  <Story name="Danger" args={{ variant: "danger", label: "Danger" }}>
    {(args) => <LightPills {...args} />}
  </Story>
</Canvas>

<ArgsTable of={LightPills} />
