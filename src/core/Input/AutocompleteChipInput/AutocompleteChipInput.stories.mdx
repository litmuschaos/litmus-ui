import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { AutocompleteChipInput } from "./AutocompleteChipInput";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { testData } from "./testData";

<Meta
  title="Core/Input/AutocompleteChipInput"
  component={AutocompleteChipInput}
/>

# AutocompleteChipInput

`AutocompleteChipInput` is a normal text input enhanced by a panel of suggested options.

## Default variants:

<Canvas>
  <Story
    name="Default"
    args={{
      onChange: (event, value) => console.log(value),
      options: testData,
      label: "Tags",
      placeholder: "Select tags",
      disableClearable: false,
      multiple: true,
      disableCloseOnSelect: true,
    }}
  >
    {(args) => (
      <div style={{ width: "20rem", height: "10rem" }}>
        <AutocompleteChipInput {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Limit tags variant:

<Canvas>
  <Story
    name="Limit"
    args={{
      onChange: (event, value) => console.log(value),
      options: testData,
      label: "Tags",
      placeholder: "Select tags",
      multiple: true,
      disableCloseOnSelect: true,
      limitTags: 2,
    }}
  >
    {(args) => (
      <div style={{ width: "40rem", height: "10rem" }}>
        <AutocompleteChipInput {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={AutocompleteChipInput} />
