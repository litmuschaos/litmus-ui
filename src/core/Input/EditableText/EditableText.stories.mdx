import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { EditableText } from "../EditableText";
import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Core/Input/EditableText" component={EditableText} />

# EditableText

`EditableText` component has been built upon InputField component and an added edit button.

## Default variant:

<Canvas>
  <Story
    name="Default"
    args={{
      label: "ProjectID",
      defaultValue: "Litmus",
    }}
  >
    {(args) => {
      return (
        <EditableText
          {...args}
          onSave={(value) => console.log(value, "saved")}
        />
      );
    }}
  </Story>
</Canvas>

<ArgsTable of={EditableText} />
