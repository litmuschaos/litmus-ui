import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Header } from "../Header";
import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta
  title="Core/Text/Header"
  component={Header}
  argTypes={{
    variant: {
      control: {
        type: "radio",
        options: {
          bold: "bold",
          small: "small",
          default: "primary",
        },
        defaultValue: "primary",
      },
    },
  }}
/>

# Header

`Header` component allows the user to give the heading.

## Default variant:

<Canvas>
  <Story name="Default" args={{}}>
    {(args) => <Header {...args}>Header Text Primary</Header>}
  </Story>
</Canvas>

## Other variants:

<Canvas>
  <Story name="Bold" args={{ variant: "bold" }}>
    {(args) => <Header {...args}>Header Text Primary </Header>}
  </Story>
  <Story name="Small" args={{ variant: "small" }}>
    {(args) => <Header {...args}>Header Text Primary </Header>}
  </Story>
  <Story name="Green" args={{ color: "green" }}>
    {(args) => <Header {...args}>Header Text Primary </Header>}
  </Story>
</Canvas>

<ArgsTable of={Header} />
