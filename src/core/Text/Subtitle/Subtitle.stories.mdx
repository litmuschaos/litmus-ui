import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { Subtitle } from "../Subtitle";
import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta
  title="Core/Text/Subtitle"
  component={Subtitle}
  argTypes={{
    variant: {
      control: {
        type: "radio",
        options: {
          bold: "bold",
          small: "small",
          default: "primary",
        },
        defaultValue: "primary",
      },
    },
  }}
/>

## Subtitle

`Buttons` allow users to take actions, and make choices, with a single tap.
`Subtitle` is a button with solid background.

## Default variant:

<Canvas>
  <Story name="Default" args={{}}>
    {(args) => <Subtitle {...args}>Subtitle Text Primary</Subtitle>}
  </Story>
</Canvas>

## Other variants:

<Canvas>
  <Story name="Bold" args={{ variant: "bold" }}>
    {(args) => <Subtitle {...args}>Subtitle Text Primary </Subtitle>}
  </Story>
  <Story name="Small" args={{ variant: "small" }}>
    {(args) => <Subtitle {...args}>Subtitle Text Primary </Subtitle>}
  </Story>
  <Story name="Green" args={{ color: "green" }}>
    {(args) => <Subtitle {...args}>Subtitle Text Primary </Subtitle>}
  </Story>
</Canvas>

<ArgsTable of={Subtitle} />
