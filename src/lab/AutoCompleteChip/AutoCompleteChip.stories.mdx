import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { AutoCompleteChip } from "./AutoCompleteChip";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { top100Films } from "./testData";

<Meta title="Lab/AutoCompleteChip" component={AutoCompleteChip} />

# AutoCompleteChip

`AutoCompleteChip` is a normal text input enhanced by a panel of suggested options.

<Canvas>
  <Story
    name="Default"
    args={{ onChange: () => console.log("hi"), optionList: top100Films }}
  >
    {(args) => (
      <div style={{ width: "30rem", height: "10rem" }}>
        <AutoCompleteChip {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={AutoCompleteChip} />
