import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { RadialChart } from "../RadialChart";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { testRadialChartData } from "./testData";

<Meta title="Lab/RadialGraphs/RadialChart" component={RadialChart} />

# RadialChart

`RadialChart` component is used to represent the
data as a Circle/SemiCircle graph. The ratio of `height:width`
for semiCircle is preferably around `1:2` (including legend table)
and circle is `1:1` (excluding the legend table)

## Default

<Canvas>
  <Story
    name="Circle-1"
    args={{
      radialData: testRadialChartData,
      heading: "heading",
      legendTableHeight: 120,
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "15rem", height: "20rem", margin: "1rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Other Variants

<Canvas>
  <Story
    name="Circle-2"
    args={{
      radialData: testRadialChartData,
      semiCircle: false,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "22rem", height: "14rem", margin: "0.5rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
  <Story
    name="SemiCircle-1"
    args={{
      radialData: testRadialChartData,
      heading: "heading",
      legendTableHeight: 95,
      showCenterHeading: true,
      semiCircle: true,
    }}
  >
    {(args) => (
      <div style={{ width: "25rem", height: "20rem", margin: "1rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
  <Story
    name="SemiCircle-2"
    args={{
      radialData: testRadialChartData,
      semiCircle: true,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "25rem", height: "15rem", margin: "1rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={RadialChart} />
