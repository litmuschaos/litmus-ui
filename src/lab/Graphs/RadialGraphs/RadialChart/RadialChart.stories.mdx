import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { RadialChart } from "../RadialChart";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { testRadialChartData } from "./testData";

<Meta title="Lab/RadialGraphs/RadialChart" component={RadialChart} />

# RadialChart

`RadialChart` component is used to represent the data as a Circle/SemiCircle graph.

## Default

<Canvas>
  <Story
    name="Circle-1"
    args={{
      radialData: testRadialChartData,
      heading: "heading",
      legendTableHeight: 120,
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
</Canvas>

## Other Variants

<Canvas>
  <Story
    name="Circle-2"
    args={{
      radialData: testRadialChartData,
      semiCircle: false,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
  <Story
    name="SemiCircle-1"
    args={{
      radialData: testRadialChartData,
      semiCircle: true,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "bottom",
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
  <Story
    name="SemiCircle-2"
    args={{
      radialData: testRadialChartData,
      semiCircle: true,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => (
      <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
        <RadialChart {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={RadialChart} />
