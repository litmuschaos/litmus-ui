import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { RadialChart } from "../RadialChart";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { testRadialChartData } from "./testData";

<Meta title="Lab/RadialChart" component={RadialChart} />

# RadialChart

`RadialChart` component is used to represent the data as a Circle/SemiCircle graph.

<Canvas>
  <Story
    name="SemiCircle-1"
    decorators={[
      (Story) => (
        <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      radialData: testRadialChartData,
      semiCircle: true,
      heading: "heading",
      legendTableHeight: 120,
      alignLegendTable: "bottom",
      showCenterHeading: true,
    }}
  >
    {(args) => <RadialChart {...args} />}
  </Story>
  <Story
    name="SemiCircle-2"
    decorators={[
      (Story) => (
        <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      radialData: testRadialChartData,
      semiCircle: true,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => <RadialChart {...args} />}
  </Story>
  <Story
    name="Circle-1"
    decorators={[
      (Story) => (
        <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      radialData: testRadialChartData,
      semiCircle: false,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "right",
      showCenterHeading: true,
    }}
  >
    {(args) => <RadialChart {...args} />}
  </Story>
  <Story
    name="Circle-2"
    decorators={[
      (Story) => (
        <div style={{ width: "20rem", height: "15rem", margin: "3rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{
      radialData: testRadialChartData,
      semiCircle: false,
      heading: "heading",
      legendTableHeight: 100,
      alignLegendTable: "bottom",
      showCenterHeading: true,
    }}
  >
    {(args) => <RadialChart {...args} />}
  </Story>
</Canvas>

<ArgsTable of={RadialChart} />
