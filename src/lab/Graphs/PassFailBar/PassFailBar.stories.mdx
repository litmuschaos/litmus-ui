import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { PassFailBar } from "../PassFailBar";
import { ArgsTable } from "@storybook/addon-docs/blocks";

<Meta title="Lab/PassFailBar" component={PassFailBar} />

## PassFailBar

`Buttons` allow users to take actions, and make choices, with a single tap.
`PassFailBar` is a button with solid background.

<Canvas>
  <Story
    name="Default"
    decorators={[
      (Story) => (
        <div style={{ width: "30rem", height: "7rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{ passPercentage: 90 }}
  >
    {(args) => <PassFailBar {...args} />}
  </Story>
</Canvas>

<ArgsTable of={PassFailBar} />
