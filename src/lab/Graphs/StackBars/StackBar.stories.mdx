import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { StackBar } from "./StackBar";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { barData, openSeries, testHandleBarClick } from "./testData";

<Meta title="Lab/StackBarGraph" component={StackBar} />

# StackBarGraph

`StackBarGraph` component is used to represent data
in form of lines, area-closed under the graph and events.

<Canvas>
  <Story
    name="Default"
    args={{
      openSeries: openSeries,
      barSeries: barData,
      unit: "%",
      yLabel: "Chaos",
      yLabelOffset: 35,
      initalxAxisDate: 0,
      handleBarClick: testHandleBarClick,
    }}
  >
    {(args) => (
      <div style={{ width: "60rem", height: "25rem" }}>
        <StackBar {...args} />
      </div>
    )}
  </Story>
</Canvas>

<ArgsTable of={StackBar} />
