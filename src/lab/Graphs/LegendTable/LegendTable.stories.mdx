import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";
import { LegendTable } from "../LegendTable";
import { ArgsTable } from "@storybook/addon-docs/blocks";
import { LegendTableTestData1 } from "./testData";

<Meta title="Lab/LegendTable" component={LegendTable} />

## LegendTable

`Buttons` allow users to take actions, and make choices, with a single tap.
`LegendTable` is a button with solid background.

<Canvas>
  <Story
    name="Default"
    decorators={[
      (Story) => (
        <div style={{ width: "30rem", height: "10rem" }}>
          <Story />
        </div>
      ),
    ]}
    args={{ data: LegendTableTestData1, heading: ["Metric Name", "Curr"] }}
  >
    {(args) => <LegendTable {...args} />}
  </Story>
</Canvas>

<ArgsTable of={LegendTable} />
